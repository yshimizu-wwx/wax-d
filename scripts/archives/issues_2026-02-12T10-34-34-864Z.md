[タイトル]: [Bug] 紐付き農家一覧表示時の React Hook ルール違反の修正
Type: Bug
Priority: High
Milestone: v0.1

概要
「紐付き農家一覧」ボタンをクリックした際、Rendered more hooks than during the previous render エラーが発生し、画面がクラッシュする問題を修正します。

現状の課題・背景
src/app/(main)/admin/users/page.tsx 内で、Hooks（特に useMemo）の呼び出し順序、または呼び出し回数がレンダリング間で変動しています。エラーログから、138行目付近の pendingColumns の定義において、条件分岐による早期リターン後に Hook が実行されている、あるいはループ内で Hook が呼ばれている可能性が高いです。

修正・実装内容
[ ] AdminUsersPage コンポーネント内の全 Hooks（useMemo, useCallback, useEffect 等）をコンポーネントの冒頭に移動する
[ ] 条件分岐（if (!data) return ... 等）よりも前に Hooks が宣言されていることを確認する
[ ] エラー箇所 pendingColumns が条件によって呼び出されたりされなかったりしていないかロジックを見直す

関連ファイル (推測)

src/app/(main)/admin/users/page.tsx

受入基準 (Doneの定義)
[ ] 「紐付き農家一覧」をクリックしてもランタイムエラーが発生せず、正常にページが表示されること

[タイトル]: [UI/UX] 案件作成画面におけるヘッダーと地図の重なり（z-index）の修正
Type: UI-UX
Priority: High
Milestone: v0.1

概要
ダッシュボードから案件作成を行う際、スクロールするとヘッダー部分に地図コンテンツが重なって表示されるレイアウト崩れを修正します。

現状の課題・背景
ヘッダー（題名部分）の z-index 設定が地図コンポーネントよりも低いため、スクロール時に地図が前面に出てしまい、ナビゲーションやタイトルが隠れてしまう状態です。

修正・実装内容
[ ] 共通ヘッダーまたは案件作成画面のスティッキーヘッダーの z-index を適切な値（例: z-50）に設定する
[ ] 地図コンポーネント（Leaflet/Google Maps等）のコンテナの z-index がヘッダーを上回らないよう調整する
[ ] スクロール時の透過設定や影（Shadow）の有無をデザインに合わせて調整する

関連ファイル (推測)

src/components/layout/Header.tsx

src/components/map/ProjectMap.tsx

src/app/(main)/provider/dashboard/page.tsx

受入基準 (Doneの定義)
[ ] 画面をスクロールした際、ヘッダーが常に最前面に表示され、地図がその下に潜り込むこと

[タイトル]: [Feature] 案件作成時の「必要な品目・作業」のマスター参照機能の実装
Type: Feature
Priority: Medium
Milestone: v0.2

概要
案件作成フローにおいて、マイページで事前に登録した「品目・作業マスター」を参照し、選択できる機能を実装します。

現状の課題・背景
現在は必要な情報を入力するセクションで、品目や作業を選択する項目が欠損しています。これらはユーザー（プロバイダ）ごとに異なるため、ハードコードではなくマイページで登録済みのマスターデータを動的に取得して表示する必要があります。

修正・実装内容
[ ] 案件作成フォームに「品目選択」および「作業内容選択」の入力項目（Select/Checkbox）を追加する
[ ] ログイン中のユーザーIDに基づき、マイページ（マスター設定）から品目・作業データをフェッチするロジックを実装する
[ ] 選択された項目を案件データの一部として保存できるよう、データベーススキーマまたはAPIエンドポイントを調整する

関連ファイル (推測)

src/app/(main)/provider/projects/create/page.tsx

lib/api/master.ts

types/project.ts

受入基準 (Doneの定義)
[ ] 案件作成画面で、ユーザーが事前に設定した品目・作業が選択肢として表示されること
[ ] 選択した内容が案件詳細に正しく保存されること

[タイトル]: [Feature] 対象エリア選択時における拠点住所の初期値設定
Type: Feature
Priority: Medium
Milestone: v0.2

概要
案件作成の「対象エリア選択」ステップにおいて、マイページに登録されている拠点の住所をデフォルト値として表示・選択されるようにします。

現状の課題・背景
現在はエリア選択が空の状態から始まるため、ユーザーが毎回住所を入力または検索する手間が発生しています。多くの案件は拠点周辺であるため、初期値として表示することでUXを向上させます。

修正・実装内容
[ ] 案件作成画面の初期ロード時に、ユーザープロフィールから拠点住所を取得する
[ ] エリア選択コンポーネントの初期状態に、取得した拠点住所をセットする
[ ] 拠点が複数ある場合は、優先拠点または選択リストから選べるように検討する（今回はまず1つ目を表示）

関連ファイル (推測)

src/app/(main)/provider/projects/create/AreaSelection.tsx

hooks/useUserProfile.ts

受入基準 (Doneの定義)
[ ] エリア選択画面を開いた際、フォームにマイページ登録済みの住所が入力済みの状態になっていること